/**
 * Page Container Components
 *
 * Standardized container wrappers for page layouts providing:
 * - Consistent responsive padding and spacing
 * - Theme-aware backgrounds and colors
 * - Maximum width constraints for readability
 * - Semantic HTML structure
 *
 * Usage:
 * import { PageContainer, SectionWrapper } from './Page Container'
 */

export { PageContainer, type PageContainerProps } from "./Page Container";
export { SectionWrapper, type SectionWrapperProps } from "./Section Wrapper";
